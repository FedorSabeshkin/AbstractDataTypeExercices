В языке Java, для ограничения переопределения методов в потомкоах, используется ключевое слово "final".
public class Human{

  public final String remember() {
    return "Я навсегда запомню этот вкусный фрукт!";
  }
}
==
Рефлексияя

Я сознательно опустил непосредсвенную реализация большинства методов, 
которая заключалась в вызове метода предка, т.к. метод предка все равно будет вызван.
Метод копирования я так же оставил, т.к. посчитал его слишком абстрактным. 